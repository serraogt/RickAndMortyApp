import React from "react";

function AddNew() {
  const pageStyle = {
    display: "flex",
    justifyContent: "center",
    alignItems: "center",
    height: "100vh",
    background: "pink",
    fontSize: "32px",
    fontWeight: "bold",
    color: "white",
  };

  return <div style={pageStyle}>Hello</div>;
}

export default AddNew;
